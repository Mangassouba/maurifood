# En local, laisser vide : le proxy Vite redirige /api vers http://localhost:4000
# En production (Vercel), pointer vers l'API déployée sur Render, ex:
VITE_API_URL=https://maurifood-api.onrender.com/api
